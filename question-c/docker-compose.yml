version: '3.8'

services:
  question-c:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: question-c-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
